#pragma once
#include <Arduino.h>

class HttpRequester {
private:

public:
    HttpRequester(/* args */);
    ~HttpRequester();
};

HttpRequester::HttpRequester(/* args */) {
}

HttpRequester::~HttpRequester() {
}
